comment:
  require_changes: true

coverage:
  status:
    patch:
      default: false
    project:
      default: false

component_management:
  individual_components:
  - component_id: tests
    name: tests
    paths:
    - tests/
    statuses:
    - type: patch
      target: 100%
    - type: project
      target: 100%

  - component_id: core
    name: core
    paths:
    - cloudbot/
    statuses:
    - type: patch
      target: 100%
    - type: project

  - component_id: plugins
    name: plugins
    paths:
    - plugins/
    statuses:
    - type: patch
    - type: project
